# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Node.js
**/node_modules
**/npm-debug.log*
**/yarn-debug.log*
**/yarn-error.log*
**/pnpm-debug.log*
lerna-debug.log*
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json
temp
*.tsbuildinfo
*.tsbuildinfo.readable.json
.yarn/install-state.gz

# Next.js specific
.next/
out/
build/

# Local environment variables
# Typically, .env.local stores sensitive keys or local overrides
.env*.local
.env.development.local
.env.test.local
.env.production.local

# Editor directories and files
.idea
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.sublime-workspace

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Thumbnails
._*

# Files that might appear in the root of a volume
.Spotlight-V100
.Trashes

# TypeScript cache
*.tsbuildinfo

# Optional: Log files
logs
*.log

# Optional: Coverage directory
coverage
.nyc_output

# Optional: Production build artifacts if you build them locally for some reason
# and don't want to commit them (though `out/` and `.next/` should cover Next.js)
dist/
public/build/ # If you have a custom build process outputting here